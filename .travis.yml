language: rust
rust:
  - stable
  - nightly
jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true
notifications:
  email: false
script:
  - cargo build
  - cargo build --verbose --all-features
  - cargo test --verbose --all-features
